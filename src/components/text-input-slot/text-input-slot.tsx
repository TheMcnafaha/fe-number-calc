import { component$ } from '@builder.io/qwik';

export interface TextInputSlotProps {

}

export const TextInputSlot = component$<TextInputSlotProps>((props) => {
  return (
    <div>
      TextInputSlot component works!
    </div>
  );
});
