import { component$ } from '@builder.io/qwik';

export interface LargeTextInputSlotProps {

}

export const LargeTextInputSlot = component$<LargeTextInputSlotProps>((props) => {
  return (
    <div>
      LargeTextInputSlot component works!
    </div>
  );
});
